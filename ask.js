const btns = document.getElementsByClassName('.box-ask-item');
const respItens = document.getElementsByClassName('.box-resp');

console.log(btns);

/* btns[0].addEventListenerListener("click", () => console.log("ok"));

for (var botao of btns){
    botao.addEventListener("click", () => console.log("ok"));
}
//console.log(btns);

//btns[0].addEventListener('click' , function() {

  //  if(respItens.style.display === "block"){
        respItens.style.display === "none"; 
 //   }else{
        respItens.style.display === "block";
  //  }
   
//});